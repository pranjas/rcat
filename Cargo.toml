[package]
name = "rcat"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
ascii = { version = "1.1.0", features = ["serde", "serde_test"] }
tempfile = "3.10.1"
